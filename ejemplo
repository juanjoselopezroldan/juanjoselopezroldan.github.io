---
layout: default
---

Text can be **bold**, _italic_, or ~~strikethrough~~. -> Estilos de letra

[Link to another page](another-page). -> enlace hacia otro lugar

There should be whitespace between paragraphs.

There should be whitespace between paragraphs. We recommend including a README, or a file with information about your project.

# [](#header-1)Header 1 -> titulo 1

This is a normal paragraph following a header. GitHub is a code hosting platform for version control and collaboration. It lets you and others work together on projects from anywhere.

## [](#header-2)Header 2 -> titulo 2

> This is a blockquote following a header. -> poner la letra en un segundo plano
>
> When something is important enough, you do it even if the odds are not in your favor.

### [](#header-3)Header 3 -> titulo 3

```js
// Javascript code with syntax highlighting. -> modo pantalla comandos con fondo negro
var fun = function lang(l) {
  dateformat.i18n = require('./lang/' + l)
  return true;
}
```

```ruby
# Ruby code with syntax highlighting
GitHubPages::Dependencies.gems.each do |gem, version|
  s.add_dependency(gem, "= #{version}")
end
```

#### [](#header-4)Header 4 -> titulo 4

*   This is an unordered list following a header. -> modo lista
*   This is an unordered list following a header.
*   This is an unordered list following a header.

##### [](#header-5)Header 5 -> titulo 5

1.  This is an ordered list following a header.
2.  This is an ordered list following a header. -> lista numerada
3.  This is an ordered list following a header.

###### [](#header-6)Header 6 -> titulo 6

| head1        | head two          | three |
|:-------------|:------------------|:------|
| ok           | good swedish fish | nice  |  -> modo tabla
| out of stock | good and plenty   | nice  |
| ok           | good `oreos`      | hmm   |
| ok           | good `zoute` drop | yumm  |

### There's a horizontal rule below this. -> titulo normal

* * * -> linea de puntos

### Here is an unordered list:

*   Item foo -> modo lista
*   Item bar
*   Item baz
*   Item zip

### And an ordered list:

1.  Item one  -> lista numerada
1.  Item two
1.  Item three
1.  Item four

### And a nested list:

- level 1 item  -> modo lista con sublistas
  - level 2 item
  - level 2 item
    - level 3 item
    - level 3 item
- level 1 item
  - level 2 item
  - level 2 item
  - level 2 item
- level 1 item
  - level 2 item
  - level 2 item
- level 1 item

### Small image

![](https://assets-cdn.github.com/images/icons/emoji/octocat.png) -> incluir imagen

### Large image

![](https://guides.github.com/activities/hello-world/branching.png) -> incluir imagen


### Definition lists can be used with HTML syntax.

<dl>    -> modo lista de datos como descripcion sobre mi
<dt>Name</dt>
<dd>Godzilla</dd>
<dt>Born</dt>
<dd>1952</dd>
<dt>Birthplace</dt>
<dd>Japan</dd>
<dt>Color</dt>
<dd>Green</dd>
</dl>

```
Long, single-line code blocks should not wrap. They should horizontally scroll if they are too long. This line should be long enough to demonstrate this. -> modo terminal
```

```
The final element. -> modo terminal final
```
